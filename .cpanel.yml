---
# cPanel Git Version Control — post-pull deployment
# Docs: each task runs in a NEW shell, so use one script (exports do NOT persist)
deployment:
  tasks:
    - /bin/chmod +x deploy/cpanel-deploy.sh
    - /bin/bash deploy/cpanel-deploy.sh
